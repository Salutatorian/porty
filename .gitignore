# Env files — never commit Strava (or other) secrets
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Vercel CLI (`vercel link`) — machine-specific link; recreate with `vercel link`
.vercel/
